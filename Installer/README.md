# VSML Installer

This is a simple installer for VSML written in Rust, using the iced GUI framework.

All it does is pull a zip file (generated by `just make-zip`) down and extract it into the game directory.

I may have gone a bit overboard with the steam game finder implementation, but oh well.
